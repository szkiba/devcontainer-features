{
  "name": "Markdown-based task runner for contributors",
  "id": "cdo",
  "version": "1.0.1",
  "description": "The `cdo` command line tool allows contributors to perform routine tasks conveniently. The markdown code blocks defining the tasks can be specified in the `CONTRIBUTING.md` or `README.md` files by default. Tasks are executed using a portable, embedded `bash`-like shell.",
  "documentationURL": "https://github.com/szkiba/cdo",
  "options": {
    "version": {
      "type": "string",
      "proposals": ["latest"],
      "default": "latest",
      "description": "Select the version to install"
    }
  }
}
